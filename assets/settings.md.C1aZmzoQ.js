import{_ as t,c as e,o as s,ag as i}from"./chunks/framework.DPDPlp3K.js";const p=JSON.parse('{"title":"Global Settings","description":"","frontmatter":{},"headers":[],"relativePath":"settings.md","filePath":"settings.md"}'),a={name:"settings.md"};function c(n,o,r,d,l,u){return s(),e("div",null,o[0]||(o[0]=[i('<h1 id="global-settings" tabindex="-1">Global Settings <a class="header-anchor" href="#global-settings" aria-label="Permalink to &quot;Global Settings&quot;">​</a></h1><p>Attribute Stock includes a few site-wide settings for further control of your stock. You can find these settings under <code>Products</code> ▹ <code>Attribute Stock</code> ▹ <code>Settings</code>.</p><h2 id="available-settings" tabindex="-1">Available settings <a class="header-anchor" href="#available-settings" aria-label="Permalink to &quot;Available settings&quot;">​</a></h2><p><strong>Modify product stock</strong> — <code>Auto</code> / <code>Everywhere</code> / <code>No</code></p><ul><li>Set whether attribute stock affects product stock availability. <code>Auto</code> will only modify stock on the frontend, in cron tasks, and during certain actions. <code>Everywhere</code> will modify stock in admin views, and may improve compatibility with other plugins. This setting also affects product stock multipliers and stock item overrides.</li></ul><p><strong>Sync product visibility</strong> — <code>Auto</code> / <code>Yes</code> / <code>No</code></p><ul><li>Update visibility taxonomy for top-level products to reflect attribute stock status. This allows product queries to respect attribute stock, for example when using the &quot;Hide out of stock items&quot; setting. <code>Auto</code> mirrors the &quot;Hide out of stock items&quot; setting.</li></ul><p><strong>Trigger product stock actions</strong> — <code>Yes</code> / <code>No</code></p><ul><li>Trigger product stock change actions when attribute stock changes. Many plugins require these actions to know when stock changes on your site. However, in some cases this might cause issues with certain plugins, or simply not be needed and use unnecessary system resources.</li></ul><p><strong>Allow backorders</strong> — <code>Do not allow</code> / <code>Allow, but notify customer</code> / <code>Allow</code></p><ul><li>Allow backorders for products using attribute stock. Does not affect products with stock management enabled, or products that are not using attribute stock. This setting can be overridden per attribute stock item.</li></ul><p><strong>Product stock format</strong> — <code>Default</code> / <code>Decimal</code> / <code>Decimal (Show whole numbers)</code></p><ul><li>Setting this to decimal allows products to use fractional stock quantities. This is required when using decimal product stock multipliers or else quantities will always be rounded down. You can optionally show whole numbers so customers don&#39;t see fractional stock quantities on the frontend.</li></ul><p><strong>Out of stock variations</strong> — <code>Out of stock</code> / <code>Greyed out</code> / <code>Hidden</code></p><ul><li>Select how variation options will be displayed when out of stock. This only applies when the <code>Hide out of stock items</code> setting is disabled, and affects all variation options site-wide regardless of attribute stock.</li><li>If your products have a lot of variations you&#39;ll need to increase the <code>Ajax variation threshold</code> for this to work.</li></ul><p><strong>Unmatched &quot;Any&quot; variations</strong> — <code>Visible</code> / <code>Out of stock</code> / <code>Greyed out</code> / <code>Hidden</code></p><ul><li>Select how &quot;Any&quot; variation options that don&#39;t match attribute stock will be displayed on the frontend. Only affects product variations with &quot;Any&quot; selected for some or all attribute terms.</li></ul><p><strong>Ajax variation threshold</strong> — Default: <code>30</code></p><ul><li>Allows changing the default WooCommerce Ajax Variation Threshold value. When products have more than this number of variations, WooCommerce will load them via Ajax. This is often problematic as it incurs extra loading times and prevents hiding or disabling out-of-stock options. Most servers running WooCommerce can handle a value of at least 300.</li></ul><p><strong>Components max depth</strong> — Default: <code>5</code></p><ul><li>The maximum number of levels to query for stock item components. E.g. for A &gt; B &gt; C, component C is at a depth of <code>2</code>. Try to set this as low as possible to maximize performance. A depth of <code>0</code> effectively disables all component stock relationships.</li></ul><h2 id="woocommerce-settings" tabindex="-1">WooCommerce settings <a class="header-anchor" href="#woocommerce-settings" aria-label="Permalink to &quot;WooCommerce settings&quot;">​</a></h2><p>In addition to the above settings, attribute stock will also respect all of the standard WooCommerce inventory settings, such as <code>Manage stock</code>, <code>Stock notifications</code>, <code>Stock thresholds</code>, <code>Out of stock visibility</code>, and <code>Stock display format</code>.▹</p>',23)]))}const m=t(a,[["render",c]]);export{p as __pageData,m as default};
